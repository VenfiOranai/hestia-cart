import { Router } from "express";

const router = Router();

// POST /api/lists/:listId/purchases   — create a purchase
// GET  /api/lists/:listId/purchases   — list all purchases for a list

export default router;
